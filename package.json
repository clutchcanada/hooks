{
  "name": "craig-common",
  "version": "1.0.3",
  "scripts": {
    "build": "rm -rf ./dist && export BABEL_ENV=production && babel src --out-dir dist --ignore 'src/**/*.test.js'",
    "build:test": "rm -rf ./dist && export BABEL_ENV=production && babel src --out-dir dist",
    "test": "./node_modules/.bin/jest --watch"
  },
  "description": "A collection of common functions I use in various projects",
  "main": "dist/index.js",
  "author": "craigpullar <craigmichaelpullar@gmail.com>",
  "license": "MIT",
  "files": ["dist"],
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.4",
    "@babel/polyfill": "^7.4.4",
    "@babel/preset-env": "^7.4.4",
    "@babel/preset-react": "^7.0.0",
    "enzyme": "^3.9.0",
    "enzyme-adapter-react-16": "^1.12.1",
    "jest": "^24.7.1"
  },
  "dependencies": {
    "ramda": "^0.26.1",
    "uuid": "^3.3.2"
  },
  "peerDependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
