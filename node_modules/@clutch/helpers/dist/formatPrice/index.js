"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _default = function _default(price) {
  var dropCents = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var options = {
    style: 'decimal',
    useGrouping: true
  };

  if (dropCents) {
    options.minimumFractionDigits = 0;
    options.maximumFractionDigits = 0;
  } else {
    options.minimumFractionDigits = 2;
    options.maximumFractionDigits = 2;
  }

  return "$".concat((price || 0.0).toLocaleString('en-CA', options));
};

exports["default"] = _default;